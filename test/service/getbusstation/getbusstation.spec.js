(function () {
  'use strict';

  describe('Service: GetBusStationService', function() {

    var GetBusStationService, $rootScope;

    beforeEach(module('buscoco.service.getbusstation'));

    beforeEach(inject(function (_$rootScope_, _GetBusStationService_) {
      GetBusStationService = _GetBusStationService_;
      $rootScope = _$rootScope_;
    }));

    describe('someThing', function() {
      it('someThing', function() {

      });
    });
  });
})();
